name: ecommerce_warehouse
version: "1.0.0"
config-version: 2

profile: ecommerce_warehouse
model-paths: ["models"]

models:
  ecommerce_warehouse:
    staging:
      +materialized: view
      +schema: staging
    intermediate:
      +materialized: view
      +schema: intermediate
    marts:
      +materialized: table
      +schema: analytics
